import { useState, useEffect } from 'react'

function ScaleDisplay({ selectedScale, scale }) {

    return (
        <div className="container">
            'hi mom'
        </div>
    )
}

export default ScaleDisplay